# crudapp
 **Simple crud app**

## Prerequisites
1. Install nodejs
2. Install mongodb
### Installing Packages
 - `npm install`
 - `bower install`
### Running
1. Start mongodb
2. cd to client `gulp serve`
3. cd to server `nodemon server` 
